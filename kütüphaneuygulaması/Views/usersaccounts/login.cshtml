
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Giriş Yap - Kütüphane</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" />
    <style>
        body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--acik-gri);
        }
        .login-card {
            max-width: 400px;
            width: 100%;
            margin: 2rem auto;
            background: var(--yumusak-beyaz);
            border-radius: 16px;
            box-shadow: 0 4px 24px rgba(30,41,59,0.10);
            padding: 2.5rem 2rem 2rem 2rem;
        }
        .login-card h2 {
            font-family: var(--font-baslik);
            color: var(--gece-laciverti);
            font-weight: 700;
            margin-bottom: 1.5rem;
            text-align: center;
        }
        .btn-primary {
            width: 100%;
            margin-top: 1rem;
        }
        .register-link {
            text-align: center;
            margin-top: 1.5rem;
        }
        .alert {
            margin-top: 1rem;
        }
    </style>
</head>


@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Giriş Yap - Kütüphane</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" />
    <style>
        body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--acik-gri);
        }
        .login-card {
            max-width: 400px;
            width: 100%;
            margin: 2rem auto;
            background: var(--yumusak-beyaz);
            border-radius: 16px;
            box-shadow: 0 4px 24px rgba(30,41,59,0.10);
            padding: 2.5rem 2rem 2rem 2rem;
        }
        .login-card h2 {
            font-family: var(--font-baslik);
            color: var(--gece-laciverti);
            font-weight: 700;
            margin-bottom: 1.5rem;
            text-align: center;
        }
        .btn-primary {
            width: 100%;
            margin-top: 1rem;
        }
        .register-link {
            text-align: center;
            margin-top: 1.5rem;
        }
        .alert {
            margin-top: 1rem;
        }
    </style>
</head>
<body>
    <div class="login-card">
        <h2>Giriş Yap</h2>
        <form asp-action="login" method="post" autocomplete="off">
            <div class="mb-3">
                <label class="form-label">Kullanıcı Adı</label>
                <input name="na" class="form-control" placeholder="Kullanıcı adınızı girin" required />
            </div>
            <div class="mb-3">
                <label class="form-label">Şifre</label>
                <input name="pa" type="password" class="form-control" placeholder="Şifrenizi girin" required />
            </div>
            <button type="submit" class="btn btn-primary">Giriş Yap</button>
            @if (ViewData["message"] != null)
            {
                <div class="alert alert-danger" role="alert">
                    @ViewData["message"]
                </div>
            }
        </form>
        <div class="register-link">
            Henüz hesabınız yok mu? <a asp-action="Create">Kayıt Ol</a>
        </div>
    </div>
</body>
</html>